# ============================================
# 前端环境变量配置示例
# ============================================
# 说明：复制此文件为 .env.local 并修改配置值
# 注意：.env.local 不会被提交到 Git（已在 .gitignore 中排除）

# --------------------------------------------
# API 配置
# --------------------------------------------
# 是否使用 Mock API（本地 LocalStorage 模拟）
# true = 使用本地 Mock 数据，false = 连接真实的 Cloudflare Workers API
VITE_USE_MOCK_API=true

# Workers API 基础 URL（当 VITE_USE_MOCK_API=false 时生效）
# 本地开发：http://127.0.0.1:8787
# 生产环境：https://your-worker.your-subdomain.workers.dev
# VITE_API_BASE_URL=http://127.0.0.1:8787

# --------------------------------------------
# Cloudflare Workers 初始化令牌（仅后端使用）
# --------------------------------------------
# 用于首次初始化系统时调用 /api/system/init 创建超级管理员账号
# 强烈建议：使用复杂的随机字符串，不要使用简单密码
# 生成方法：openssl rand -base64 32
# INIT_TOKEN=CHANGE_ME_TO_A_SECURE_RANDOM_STRING
